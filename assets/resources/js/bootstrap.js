// ---

// Initialize jQuery globally
window.jQuery = require('jquery'); // Required for OctoberCMS AJAX framework

// Initialize UIkit globally
window.UIkit = require('uikit');
window.UIkit.use(require('uikit/dist/js/uikit-icons')); // Adds support for UIkit iconography